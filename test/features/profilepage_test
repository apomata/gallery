require 'test_helper'

class ProfileTest < ActiveSupport::TestCase
	# test "the truth" do
	#   assert true
	# end
	test "at page" do
		visit(profiles_path)
		expect(page).to have_content('Listing profiles')
	end



end